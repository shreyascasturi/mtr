-- Want to create an app for spotify that will:

-- write all my current albums + songs out to a text file 
    -- delete all albums + songs
-- read all my albums + songs from a text file
    -- and add them back into spotify

-- Call it SAVER

-- connectAccount :: String -> String -> Token/Result
-- Need to be able to connect to Spotify with
-- username, password
-- Probably get some sort of token or authorization.

-- dumpAll :: JSON/String call -> String (name of file) [file will also be written, that's probably a Monad or something]

-- readAll :: file path/String -> [String] (List of all albums)?

